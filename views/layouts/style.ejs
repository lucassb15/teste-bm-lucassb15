<style>
  * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
  }

  body {
    background-color: #eef0f6;
    font-family: "Open Sans", sans-serif;
  }

  .section-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin: 40px;

  }

  #login .container {
    margin-top: 5rem;
    width: 40%;
  }

  #register .container {
    width: 45%;
  }

  .container-uinfo {
    display: flex;
    flex-direction: column;
    padding: 2rem 4rem;
    width: 100%;
    background-color: #231f20;
    border-radius: 4px;
  }

  label {
    color: #eef0f6;
    font-size: medium;
    width: auto;
    -webkit-user-select: none;
    /* Safari */
    -ms-user-select: none;
    /* IE 10 and IE 11 */
    user-select: none;
    /* Standard syntax */
  }

  input:focus,
  select:focus {
    outline: none;
    border-bottom: 1px solid #14dbe3;
  }

  input,
  select {
    padding: 0.5rem 2rem;
    text-align: left;
    width: 100%;
    padding: 12px 20px;
    margin: 8px 0;
    border: 1px solid #ccc;
    border-radius: 4px;
  }

  input:hover {
    background-color: #dddddd;
  }

  .alert-msg {
    text-align: center;
    font-size: 1rem;
    margin: 10px;
  }

  .alert-login {
    color: #ca3232;
    position: absolute;
    margin-top: 20px;
    margin-left: 35rem;

    text-align: center;
  }
  .alert-register-sucess {
    color: green;
    position: absolute;
    margin-top: 20px;
    margin-left: 35rem;
    text-align: center;
  }
  .btn-style {
    text-align: center;
    margin-top: 1rem;
    padding: 12px 20px;
    text-transform: uppercase;
    color: #231f20;
    background-color: #14dbe3;
    border-radius: 4px;
    border: 1px solid #14dbe3;
    font-weight: 700;
    font-size: 0.8rem;
    letter-spacing: 2px;
    cursor: pointer;
    transition: background 1s;
  }

  .btn-style:hover {
    background-color: #0d878b;
    border: 1px solid #0e989c;
    color: #292626;
  }

  .register-link {
    display: flex;
    flex-direction: row;
    justify-content: center;
    margin-top: 20px;
    gap: .2rem;
    font-size: .8rem;
  }

  .subtitle {
    color: #eef0f6;
  }

  a.subtitle {
    color: #14dbe3;
    text-decoration: none;
    transition: color 1s;
  }

  a.subtitle:hover {
    color: #0d878b;
    text-decoration: none;
  }

  /* Register css */

  .title {
    color: #eef0f6;
    font-weight: 600;
    margin-bottom: 15px;
    text-align: center;
  }

  @media (max-width: 767px) {
    #login .container {
      margin-top: 5rem;
      width: 100%;
    }

    #register .container {
      margin-top: 5rem;
      width: 100%;
    }
  }
</style>